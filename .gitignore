# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# 旧的 RandomForest 模型体积过大（874MB / 323MB），不入库
*.pkl
# 新的 LightGBM 模型只有 1.6MB，直接提交，clone 下来即可运行
!model/price_model.pkl

# 数据源以 vehicle.csv.gz 的形式入库（11.9MB -> 1.2MB，pandas 直接读）。
# 解压后的副本和各处重复数据集都不提交。
/vehicle.csv
data/vehicle.csv
model/vehicle.csv

# Flask / env
.env
instance/
templates/cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code
.claude/
